import { Gemunu_Libre, Open_Sans } from '@next/font/google';

export const gemunu = Gemunu_Libre({
  subsets: ['latin'],
  variable: '--font-gemunu',
});

export const open = Open_Sans({
  subsets: ['latin'],
  variable: '--font-open',
});
